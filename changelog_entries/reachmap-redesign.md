### UI
   * Reachmap redesign.
     * Rounded the borders a bit.
     * Changed default color from blue to yellow (easier to see on ice/snow tiles).
     * Decreased default opacity a bit, but implemented auto strengthening for bright tiles and enemy unit tiles.
     * Re-added darkening of unreachable tiles to increase contrast. It scales with the reachmap tint fill slider advanced option. 
	 
