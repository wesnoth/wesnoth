{GENERIC_UNIT_TEST "store_locations_one" (
    [event]
        name = start
        [store_locations]
            x,y=1,1
            variable=tmp
        [/store_locations]
        {RETURN ({VARIABLE_CONDITIONAL tmp.terrain equals "Gg^Kov"})}
    [/event]
)}
