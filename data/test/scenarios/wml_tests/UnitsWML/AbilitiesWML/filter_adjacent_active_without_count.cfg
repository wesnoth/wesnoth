#textdomain wesnoth-test

#####
# API(s) being tested: ability[filter_adjacent]adjacent,count=
##
# Actions:
# Give Alice an ability specialX, which is only active if one adjacent unit is bob, without specify 'count'.
# Test whether the ability is active.
##
# Expected end state:
# specialX should be active because 'count' attribute not need to be specified but trigger a deprecation warning (a BROKE_STRICT result).
#####
{COMMON_KEEP_A_B_C_D_UNIT_TEST "filter_adjacent_active_without_count" (
    [event]
        name=start

        [object]
            silent=yes
            [effect]
                apply_to=new_ability
                [abilities]
                    [damage]
                        id=specialX
                        name=_ "specialX"
                        description=_ "specialX is active if and only if one of the adjacents units is bob"
                        value=100
                        apply_to=self
                        [filter_adjacent]
                            adjacent=n,ne,se,s,sw,nw
                            id=bob
                        [/filter_adjacent]
                    [/damage]
                [/abilities]
            [/effect]
            [filter]
                id=alice
            [/filter]
        [/object]

        {ASSERT (
            [have_unit]
                ability_id_active=specialX
            [/have_unit]
        )}

        {SUCCEED}
    [/event]
)}
