#textdomain wesnoth-test

#####
# API(s) being tested: ability[filter_adjacent_location]adjacent,count=
##
# Actions:
# Give Alice an ability specialX, which is only active if exactly three adjacent terrains are Gg, directions are not specified.
# Test whether the ability is active.
##
# Expected end state:
# specialX should be active.
# A deprecation warning is triggered (a BROKE_STRICT result).
#####
{COMMON_KEEP_A_B_C_D_UNIT_TEST "filter_adjacent_location_count_three_without_direction_active" (
    [event]
        name=start

        [object]
            silent=yes
            [effect]
                apply_to=new_ability
                [abilities]
                    [damage]
                        id=specialX
                        name=_ "specialX"
                        description=_ "specialX is active if and only if 3 adjacent hexes are Gg"
                        value=100
                        apply_to=self
                        [filter_adjacent_location]
                            count=3
                            terrain=Gg
                        [/filter_adjacent_location]
                    [/damage]
                [/abilities]
            [/effect]
            [filter]
                id=alice
            [/filter]
        [/object]

        {ASSERT (
            [have_unit]
                ability_id_active=specialX
            [/have_unit]
        )}

        {SUCCEED}
    [/event]
)}
