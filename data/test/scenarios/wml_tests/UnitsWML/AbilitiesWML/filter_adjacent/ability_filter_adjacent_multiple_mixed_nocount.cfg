#textdomain wesnoth-test

#####
# API(s) being tested: ability[filter_adjacent]adjacent,count=
##
# Actions:
# Give Alice an ability specialX, which is only active if Bob in the hexes south and south-west of Alice
# Test whether the ability is active, omitting the count= attribute.
##
# Expected end state:
# On 1.18, specialX is inactive, because omitting count= automatically fails.
# On 1.19.26, specialX is inactive, because the compat layer sets count to the number of directions.
#####
{COMMON_KEEP_A_B_C_D_UNIT_TEST "ability_filter_adjacent_multiple_mixed_nocount" (
    [event]
        name=start

        [object]
            silent=yes
            [effect]
                apply_to=new_ability
                [abilities]
                    [damage]
                        id=specialX
                        name=_ "specialX"
                        description=_ "specialX is active if and only if Bob in the hexes south and south-west of Alice"
                        value=100
                        apply_to=self
                        [filter_adjacent]
                            adjacent=s,sw
                            id=bob
                        [/filter_adjacent]
                    [/damage]
                [/abilities]
            [/effect]
            [filter]
                id=alice
            [/filter]
        [/object]

        {ASSERT (
            [not]
                [have_unit]
                    ability_id_active=specialX
                [/have_unit]
            [/not]
        )}

        {SUCCEED}
    [/event]
)}
