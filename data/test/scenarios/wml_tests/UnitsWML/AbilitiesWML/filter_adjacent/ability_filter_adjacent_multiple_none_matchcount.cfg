#textdomain wesnoth-test

#####
# API(s) being tested: ability[filter_adjacent]adjacent,count,race=
##
# Actions:
# Give Alice an ability specialX, which is only active if there are elves south and south-west of Alice
# Test whether the ability is active, passing the number of expected matches as count=.
##
# Expected end state:
# specialX should be inactive on Wesnoth 1.18, as the existing behavior requires both hexes to match, even with count=0.
# specialX should be active on Wesnoth 1.20. TODO: that's arguable. An explicit count= is given, and the code is meaningless on 1.18, so compatibility is unnecessary.
#####
{COMMON_KEEP_A_B_C_D_UNIT_TEST "ability_filter_adjacent_multiple_none_matchcount" (
    [event]
        name=start

        [object]
            silent=yes
            [effect]
                apply_to=new_ability
                [abilities]
                    [damage]
                        id=specialX
                        name=_ "specialX"
                        description=_ "specialX is active if and only if there are elves south and south-west of Alice"
                        value=100
                        apply_to=self
                        [filter_adjacent]
                            adjacent=s,sw
                            count=0
                            race=elf
                        [/filter_adjacent]
                    [/damage]
                [/abilities]
            [/effect]
            [filter]
                id=alice
            [/filter]
        [/object]

        {ASSERT (
            [not]
                [have_unit]
                    ability_id_active=specialX
                [/have_unit]
            [/not]
        )}

        {SUCCEED}
    [/event]
)}

