# wmllint: no translatables

#####
# API(s) being tested: [leadership]min_value=
##
# Actions:
# Give all units 50% self-leadership bonus damage with a min_value=10
# Attack each other
##
# Expected end state:
# min_value works
# The damage from the attack is increased by 10% (level *10)
#####
{COMMON_KEEP_A_B_UNIT_TEST "leadership_wfl_min_value" (
    [event]
        name = start

        [modify_unit]
            [filter]
            [/filter]
            [effect]
                apply_to = new_ability
                [abilities]
                    {TEST_ABILITY leadership 5 (min_value="(level*10)") SELF=yes}
                [/abilities]
            [/effect]
        [/modify_unit]

        {ATTACK_AND_VALIDATE 110}
        {SUCCEED}
    [/event]
)}
