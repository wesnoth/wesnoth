# wmllint: no translatables

#####
# API(s) being tested: [teleport]
##
# Actions:
# 
##
# Expected end state:
# 
#####
{COMMON_KEEP_A_B_UNIT_TEST "teleport" (
    [event]
        name = start

        [modify_unit]
            [filter]
            [/filter]
            [effect]
                apply_to = new_ability
                [abilities]
                    {TEST_ABILITY_NO_VALUE teleport ()}
                [/abilities]
            [/effect]
        [/modify_unit]

        [move_unit]
            id=alice
            to_x=3
            to_y=3
        [/move_unit]
    [/event]

    [event]
        name=moveto

        [store_unit]
            [filter]
            [/filter]
            variable=temp
        [/store_unit]

        #{ASSERT {VARIABLE_CONDITIONAL temp[1].status.uncovered boolean_equals no}}
        #{SUCCEED}
    [/event]
)}
