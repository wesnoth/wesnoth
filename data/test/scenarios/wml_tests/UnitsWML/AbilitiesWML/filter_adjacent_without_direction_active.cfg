#textdomain wesnoth-test

#####
# API(s) being tested: ability[filter_adjacent]adjacent,count=
##
# Actions:
# Give Alice an ability specialX, which is only active if one adjacent unit is bob, direction is not specified.
# Test whether the ability is active.
##
# Expected end state:
# specialX should be active.
# A deprecation warning is triggered (a BROKE_STRICT result).
#####
{COMMON_KEEP_A_B_C_D_UNIT_TEST "filter_adjacent_without_direction_active" (
    [event]
        name=start

        [object]
            silent=yes
            [effect]
                apply_to=new_ability
                [abilities]
                    [damage]
                        id=specialX
                        name=_ "specialX"
                        description=_ "specialX is active if and only if one of the adjacent units is bob"
                        value=100
                        apply_to=self
                        [filter_adjacent]
                            id=bob
                            count=1-6
                        [/filter_adjacent]
                    [/damage]
                [/abilities]
            [/effect]
            [filter]
                id=alice
            [/filter]
        [/object]

        {ASSERT (
            [have_unit]
                ability_id_active=specialX
            [/have_unit]
        )}

        {SUCCEED}
    [/event]
)}
