{GENERIC_UNIT_TEST "scatter_units" (
    [event]
        name=start
        {SCATTER_UNITS 3 (Mage) 1 () side=1}
        {RETURN (
            [have_unit]
                type=Mage
                side=1
                count=3
            [/have_unit]
        )}
    [/event]

)}
