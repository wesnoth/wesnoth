faifile 'scouting_eval.fai'

if(me.loc = my_leader.loc,
	-5,
if(size(shroud) = 0, 
	-5,
if(size(enemies_in_range) != 0,
	-5,
if(me.total_movement > 5, 
	50, 
	-5))))
where enemies_in_range = filter(unit_moves(me.loc),units_can_reach(enemy_moves, self)),
      shroud   = find_shroud(),

faiend
